# ПРОМПТ 12.2: Обновление production-конфигурации (Prisma + PostgreSQL)

> Запускать в папке `/home/gpaul/Obsidian/ecokon.ru/ecokon`
> Перед запуском: убедись что промпт 12.1 выполнен (Prisma + NextAuth работают локально)
> Требуется доступ к `/home/gpaul/Obsidian/pdnguard` (для reference)

---

```
Обнови production-конфигурацию проекта ecokon.ru после миграции с Supabase на Prisma + NextAuth + PostgreSQL. Нужно добавить PostgreSQL контейнер в продакшн, обновить Dockerfile и CI/CD.

== КОНТЕКСТ ==

После промпта 12.1 проект мигрировал на:
- Prisma ORM вместо Supabase клиента
- NextAuth.js вместо Supabase Auth
- Локальный PostgreSQL вместо Supabase cloud

Production архитектура (VPS):
- Общая Docker-сеть `webproxy` (external)
- pdnguard.ru работает на :3000/:3001 + свой PostgreSQL (:5432)
- ecokon.ru работает на :3002
- Nginx обслуживает оба домена
- CI/CD: GitHub Actions → GHCR → SSH deploy

Текущие файлы для обновления:
- docker-compose.prod.yml — нет PostgreSQL контейнера, есть Supabase env vars
- Dockerfile — нет prisma generate, есть Supabase build args
- .github/workflows/deploy.yml — нужно обновить env vars

== ЗАДАЧИ ==

### 1. Обнови docker-compose.prod.yml

Путь: `docker-compose.prod.yml`

```yaml
services:
  postgres:
    image: postgres:16-alpine
    container_name: ecokon-db
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-ecokon}
      POSTGRES_USER: ${POSTGRES_USER:-ecokon}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ecokon_pgdata:/var/lib/postgresql/data
    networks:
      - internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ecokon}"]
      interval: 10s
      timeout: 5s
      retries: 5

  web:
    image: ghcr.io/pavelgla/ecokon/web:${IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_SITE_URL: https://ecokon.ru
        NEXT_PUBLIC_YMAPS_API_KEY: ${NEXT_PUBLIC_YMAPS_API_KEY:-}
        NEXT_PUBLIC_METRIKA_ID: ${NEXT_PUBLIC_METRIKA_ID:-}
        NEXT_PUBLIC_VK_PIXEL_ID: ${NEXT_PUBLIC_VK_PIXEL_ID:-}
    container_name: ecokon-web
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      NODE_ENV: production
      DATABASE_URL: postgresql://${POSTGRES_USER:-ecokon}:${POSTGRES_PASSWORD}@ecokon-db:5432/${POSTGRES_DB:-ecokon}
      NEXTAUTH_URL: https://ecokon.ru
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET}
      YOOKASSA_SHOP_ID: ${YOOKASSA_SHOP_ID}
      YOOKASSA_SECRET_KEY: ${YOOKASSA_SECRET_KEY}
      APISHIP_API_KEY: ${APISHIP_API_KEY}
      APISHIP_FROM_CITY_ID: ${APISHIP_FROM_CITY_ID}
      APISHIP_PLATFORM_ID: ${APISHIP_PLATFORM_ID:-}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      TELEGRAM_CHAT_ID: ${TELEGRAM_CHAT_ID}
      BREVO_API_KEY: ${BREVO_API_KEY}
      BREVO_SENDER_EMAIL: ${BREVO_SENDER_EMAIL:-noreply@ecokon.ru}
    networks:
      - webproxy
      - internal

volumes:
  ecokon_pgdata:

networks:
  webproxy:
    external: true
  internal:
    driver: bridge
```

Ключевые изменения:
- Добавлен `postgres` контейнер с healthcheck
- Удалены SUPABASE_SERVICE_ROLE_KEY, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY
- Добавлены DATABASE_URL, NEXTAUTH_URL, NEXTAUTH_SECRET
- Добавлена внутренняя сеть `internal` (postgres не виден извне, только web → postgres)
- web зависит от postgres (depends_on + service_healthy)
- PostgreSQL НЕ подключён к webproxy — доступен только внутренне

### 2. Обнови Dockerfile

Путь: `Dockerfile`

```dockerfile
# Stage 1: Dependencies
FROM node:20-alpine AS deps
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --ignore-scripts

# Stage 2: Build
FROM node:20-alpine AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .

# Prisma generate (нужен до build)
RUN npx prisma generate

# Build args для клиентских env
ARG NEXT_PUBLIC_SITE_URL=https://ecokon.ru
ARG NEXT_PUBLIC_YMAPS_API_KEY
ARG NEXT_PUBLIC_METRIKA_ID
ARG NEXT_PUBLIC_VK_PIXEL_ID

ENV NEXT_PUBLIC_SITE_URL=${NEXT_PUBLIC_SITE_URL}
ENV NEXT_PUBLIC_YMAPS_API_KEY=${NEXT_PUBLIC_YMAPS_API_KEY}
ENV NEXT_PUBLIC_METRIKA_ID=${NEXT_PUBLIC_METRIKA_ID}
ENV NEXT_PUBLIC_VK_PIXEL_ID=${NEXT_PUBLIC_VK_PIXEL_ID}

RUN npm run build

# Stage 3: Production
FROM node:20-alpine AS runner
WORKDIR /app
ENV NODE_ENV=production

RUN addgroup --system --gid 1001 nodejs && \
    adduser --system --uid 1001 nextjs

COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static
COPY --from=builder /app/public ./public

# Prisma: копируем клиент и schema для миграций
COPY --from=builder /app/node_modules/.prisma ./node_modules/.prisma
COPY --from=builder /app/node_modules/@prisma ./node_modules/@prisma
COPY --from=builder /app/prisma ./prisma

USER nextjs
EXPOSE 3002
ENV PORT=3002
ENV HOSTNAME="0.0.0.0"

HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
  CMD wget --no-verbose --tries=1 --spider http://127.0.0.1:3002/ || exit 1

CMD ["node", "server.js"]
```

Ключевые изменения:
- Удалены ARG/ENV для NEXT_PUBLIC_SUPABASE_URL и NEXT_PUBLIC_SUPABASE_ANON_KEY
- Добавлен `npx prisma generate` перед `npm run build`
- Копируются Prisma client artifacts (node_modules/.prisma, node_modules/@prisma)
- Копируется prisma/ директория (для миграций при деплое)

### 3. Создай скрипт для миграций при деплое

Путь: `scripts/deploy-migrate.sh`

```bash
#!/bin/sh
set -e

echo "Running Prisma migrations..."
npx prisma migrate deploy

echo "Starting application..."
exec node server.js
```

Обнови CMD в Dockerfile если хочешь автоматические миграции при старте:
```dockerfile
# Вариант A: миграции при старте (проще, но медленнее холодный старт)
COPY --from=builder /app/scripts/deploy-migrate.sh ./scripts/deploy-migrate.sh
CMD ["sh", "scripts/deploy-migrate.sh"]

# Вариант B: миграции отдельным шагом в CI/CD (рекомендуется)
CMD ["node", "server.js"]
```

Рекомендация: используй Вариант B — миграции в CI/CD (шаг 5).

### 4. Создай .env.production.example

Путь: `.env.production.example`

```env
# === Database ===
POSTGRES_DB=ecokon
POSTGRES_USER=ecokon
POSTGRES_PASSWORD=CHANGE_ME_strong_password_here

# === NextAuth ===
NEXTAUTH_URL=https://ecokon.ru
NEXTAUTH_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_32

# === YooKassa ===
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=

# === ApiShip ===
APISHIP_API_KEY=
APISHIP_FROM_CITY_ID=
APISHIP_PLATFORM_ID=

# === Telegram ===
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# === Brevo Email ===
BREVO_API_KEY=
BREVO_SENDER_EMAIL=noreply@ecokon.ru

# === Public (build args) ===
NEXT_PUBLIC_SITE_URL=https://ecokon.ru
NEXT_PUBLIC_YMAPS_API_KEY=
NEXT_PUBLIC_METRIKA_ID=
NEXT_PUBLIC_VK_PIXEL_ID=

# === Docker ===
IMAGE_TAG=latest
```

### 5. Обнови GitHub Actions workflow

Путь: `.github/workflows/deploy.yml`

Обнови workflow с учётом Prisma-миграций. Основные изменения:

```yaml
name: Deploy ecokon.ru

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: pavelgla/ecokon/web

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: ecokon_test
          POSTGRES_USER: ecokon
          POSTGRES_PASSWORD: test_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx prisma generate
      - run: npx prisma migrate deploy
        env:
          DATABASE_URL: postgresql://ecokon:test_password@localhost:5432/ecokon_test
      - run: npm run build
        env:
          DATABASE_URL: postgresql://ecokon:test_password@localhost:5432/ecokon_test
          NEXTAUTH_SECRET: test-secret-for-build
          NEXTAUTH_URL: http://localhost:3002

  build:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          build-args: |
            NEXT_PUBLIC_SITE_URL=https://ecokon.ru
            NEXT_PUBLIC_YMAPS_API_KEY=${{ secrets.NEXT_PUBLIC_YMAPS_API_KEY }}
            NEXT_PUBLIC_METRIKA_ID=${{ secrets.NEXT_PUBLIC_METRIKA_ID }}
            NEXT_PUBLIC_VK_PIXEL_ID=${{ secrets.NEXT_PUBLIC_VK_PIXEL_ID }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd ~/ecokon

            # Pull latest image
            docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}

            # Update image tag
            export IMAGE_TAG=${{ github.sha }}

            # Run Prisma migrations
            docker compose -f docker-compose.prod.yml run --rm web npx prisma migrate deploy

            # Restart services
            docker compose -f docker-compose.prod.yml up -d --remove-orphans

            # Cleanup old images
            docker image prune -f
```

Ключевые изменения vs старый workflow:
- Удалены secrets для SUPABASE (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY)
- Добавлен PostgreSQL service в test job
- Добавлен шаг `prisma generate` + `prisma migrate deploy` в test
- В deploy: выполняется `prisma migrate deploy` перед стартом
- Build args не содержат Supabase переменных

### 6. Обнови Nginx конфигурацию для ecokon.ru

Путь: на VPS в конфиге Nginx pdnguard (файл уже должен существовать из промпта Deploy).
Если конфиг ещё не создан, добавь server block:

Файл на VPS: `~/pdnguard/deploy/nginx/conf.d/ecokon.conf`

```nginx
upstream ecokon_web {
    server ecokon-web:3002;
}

server {
    listen 80;
    server_name ecokon.ru www.ecokon.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name ecokon.ru www.ecokon.ru;

    ssl_certificate /etc/letsencrypt/live/ecokon.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ecokon.ru/privkey.pem;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;

    location / {
        proxy_pass http://ecokon_web;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Static files cache
    location /_next/static {
        proxy_pass http://ecokon_web;
        proxy_cache_valid 200 60d;
        add_header Cache-Control "public, immutable, max-age=31536000";
    }
}
```

### 7. Создай скрипт первоначального развёртывания

Путь: `scripts/initial-deploy.sh` (для запуска на VPS один раз)

```bash
#!/bin/bash
set -e

echo "=== Initial deploy ecokon.ru ==="

# 1. Создать Docker network если не существует
docker network create webproxy 2>/dev/null || echo "Network webproxy already exists"

# 2. Создать директорию
mkdir -p ~/ecokon
cd ~/ecokon

# 3. Скопируй .env из .env.production.example и заполни значения
# cp .env.production.example .env
# nano .env

# 4. Запусти PostgreSQL
docker compose -f docker-compose.prod.yml up -d postgres

# 5. Подожди пока PostgreSQL будет готов
echo "Waiting for PostgreSQL..."
sleep 5

# 6. Запусти миграции
docker compose -f docker-compose.prod.yml run --rm web npx prisma migrate deploy

# 7. Засей БД (только первый раз!)
docker compose -f docker-compose.prod.yml run --rm web npx prisma db seed

# 8. Запусти web
docker compose -f docker-compose.prod.yml up -d

# 9. Получи SSL (если ещё нет)
# docker compose -f ~/pdnguard/docker-compose.prod.yml run --rm certbot certonly --webroot -w /var/www/certbot -d ecokon.ru -d www.ecokon.ru

# 10. Перезагрузи Nginx
# docker exec pdnguard-nginx nginx -s reload

echo "=== Deploy complete ==="
echo "Check: curl -I https://ecokon.ru"
```

### 8. Обнови .gitignore

Добавь если отсутствуют:

```
# Prisma
prisma/migrations/migration_lock.toml

# Environment
.env
.env.local
.env.production

# Docker dev
docker-compose.yml
```

НЕ добавляй `prisma/migrations/` — миграции должны быть в git!

### 9. Обнови .dockerignore

```
node_modules
.next
.git
.github
.env*
docker-compose.yml
docker-compose.prod.yml
README.md
docs/
supabase/
```

### 10. GitHub Secrets для обновления

Удали из GitHub Secrets:
- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY
- SUPABASE_SERVICE_ROLE_KEY

Добавь в GitHub Secrets:
- NEXTAUTH_SECRET (сгенерируй: openssl rand -base64 32)
- POSTGRES_PASSWORD (сильный пароль)

Оставь существующие:
- VPS_HOST, VPS_USER, VPS_SSH_KEY
- YOOKASSA_SHOP_ID, YOOKASSA_SECRET_KEY
- APISHIP_API_KEY, APISHIP_FROM_CITY_ID
- TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID
- BREVO_API_KEY
- NEXT_PUBLIC_YMAPS_API_KEY, NEXT_PUBLIC_METRIKA_ID, NEXT_PUBLIC_VK_PIXEL_ID

### 11. Бэкап PostgreSQL (настрой на VPS)

Создай cron job для ежедневного бэкапа:

Путь: `scripts/backup-db.sh`

```bash
#!/bin/bash
BACKUP_DIR=~/backups/ecokon
mkdir -p $BACKUP_DIR
DATE=$(date +%Y%m%d_%H%M)
docker exec ecokon-db pg_dump -U ecokon ecokon | gzip > $BACKUP_DIR/ecokon_$DATE.sql.gz
# Удалять бэкапы старше 30 дней
find $BACKUP_DIR -name "*.sql.gz" -mtime +30 -delete
echo "Backup done: ecokon_$DATE.sql.gz"
```

```bash
# На VPS:
chmod +x ~/ecokon/scripts/backup-db.sh
crontab -e
# Добавь: 0 3 * * * ~/ecokon/scripts/backup-db.sh >> ~/backups/ecokon/backup.log 2>&1
```

== КРИТЕРИИ ВЫПОЛНЕНИЯ ==

1. ✅ docker-compose.prod.yml содержит postgres + web сервисы
2. ✅ PostgreSQL изолирован во внутренней сети (не виден извне)
3. ✅ web зависит от postgres (depends_on + healthcheck)
4. ✅ Dockerfile содержит `prisma generate` и копирует Prisma artifacts
5. ✅ Нет Supabase env vars ни в docker-compose, ни в Dockerfile, ни в CI/CD
6. ✅ GitHub Actions: test с PostgreSQL service, prisma migrate в deploy
7. ✅ .env.production.example содержит все нужные переменные
8. ✅ Nginx конфиг для ecokon.ru готов (SSL + proxy + security headers)
9. ✅ Скрипт бэкапа PostgreSQL
10. ✅ docker compose -f docker-compose.prod.yml up -d работает локально
```
